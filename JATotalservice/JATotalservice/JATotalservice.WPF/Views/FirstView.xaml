<views:MvxWpfView x:Class="JATotalservice.WPF.Views.FirstView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="12,0,12,0">
 
            <TextBlock Text="{Binding Hello}" />
            <Button Command="{Binding NavigateToMaterialsCommand}" Width="100" Height="100" Content="Material"/>
            <Button Command="{Binding NavigateToMaterialsCommand}" Width="100" Height="100" Content="Tidsregistering"/>
            <Button Command="{Binding NavigateToTasksCommand}" Width="100" Height="100" Content="Opgaver"/>
        </StackPanel>
    </Grid>
</views:MvxWpfView>
   